-- Recorded in production migration history without its SQL (applied
-- outside the tracked migration tooling); the repository copy was corrupted by
-- shell escaping ($func$ quotes lost) and never ran as written. The effect is
-- captured in supabase/schema/production_schema.sql; the corrupted draft is kept in
-- archive/pre-alignment-2026-09-24/.
SELECT 1;
