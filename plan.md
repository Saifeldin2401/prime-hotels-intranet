1. **Analyze build/deployment failure on Netlify**. Looking at earlier logs, I can see that `npm ci` fails or Vite build fails. Let me do `npm ci` to see if it works.
2. Ensure everything builds correctly locally.
