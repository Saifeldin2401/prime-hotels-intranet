/**
 * Authentication & user account hooks barrel export.
 * Import from '@/hooks/auth' for organized access.
 */
export { useAuth } from '../useAuth'
export { useAccountActions } from '../useAccountActions'
export { useInactivityTimeout } from '../useInactivityTimeout'
export { usePermissions } from '../usePermissions'
