                // 3. New Hires (Query profiles instead of user_properties for created_at)
                (async () => {
                    if (propUserIds.length === 0) return { count: 0 }
                    return supabase
                        .from('profiles')
                        .select('id', { count: 'exact', head: true })
                        .gte('created_at', startOfMonth.toISOString())
                        .in('id', propUserIds)
                })(),
